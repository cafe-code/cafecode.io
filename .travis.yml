env:
  - github_token=rBrT0xHFc1noqvJoSHbiew

# Clean and don't fail
install:
  - rm -rf public || exit 0

# Build the website
script:
  - cd hugo
  - ../binaries/hugo

# Deploy to GitHub pages
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: public
  github_token: $github_token
  on:
    branch: master